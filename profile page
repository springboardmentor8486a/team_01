<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CleanStreet - Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f5f7fa;
      font-family: Arial, sans-serif;
    }
    .sidebar {
      background-color: #fff;
      height: 100vh;
      padding: 20px;
      border-right: 1px solid #ddd;
    }
    .sidebar .profile-pic {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background: #e9ecef;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 40px;
      margin: 0 auto 10px;
      position: relative;
    }
    .sidebar .profile-pic input {
      display: none;
    }
    .sidebar .profile-pic label {
      position: absolute;
      bottom: 0;
      right: 25px;
      background: #00000088;
      color: #fff;
      padding: 5px;
      border-radius: 50%;
      cursor: pointer;
      font-size: 14px;
    }
    .nav-link {
      font-weight: 500;
      margin-right: 15px;
    }
    .profile-card {
      background: #e9f1fb;
      padding: 20px;
      border-radius: 6px;
    }
    .profile-card .form-control[disabled] {
      background: #dcdcdc;
      opacity: 1;
    }
  </style>
</head>
<body>
<div class="container-fluid">
  <div class="row">
    <!-- Sidebar -->
    <div class="col-md-3 sidebar text-center">
      <div class="profile-pic">
        <img id="profileImage" src="https://via.placeholder.com/100" alt="Profile" class="rounded-circle" width="100" height="100">
        <label for="uploadPic">📷</label>
        <input type="file" id="uploadPic" accept="image/*">
      </div>
      <h5 id="sidebarName">User</h5>
      <p class="text-muted" id="sidebarUsername">@demo_user</p>
      <button class="btn btn-primary btn-sm mb-3">Citizen</button>
      <p class="text-muted small">Active citizen helping to improve our community through CleanStreet</p>
    </div>

    <!-- Main Content -->
    <div class="col-md-9 p-4">
      <!-- Top Nav -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h4>CleanStreet</h4>
        <div>
          <a href="#" class="nav-link d-inline">Dashboard</a>
          <a href="#" class="nav-link d-inline text-dark">Profile</a>
        </div>
      </div>

      <!-- Profile Info -->
      <div class="profile-card">
        <h5>Profile</h5>
        <p class="text-muted">Manage your account information and preferences</p>
        <form id="profileForm">
          <div class="row mb-3">
            <div class="col-sm-6">
              <label class="form-label">Username</label>
              <input type="text" class="form-control" id="username" value="demo_user" disabled>
            </div>
            <div class="col-sm-6">
              <label class="form-label">Email</label>
              <input type="email" class="form-control" id="email" value="demouser@gmail.com" disabled>
            </div>
          </div>
          <div class="row mb-3">
            <div class="col-sm-6">
              <label class="form-label">Full Name</label>
              <input type="text" class="form-control" id="fullname" value="Demo User" disabled>
            </div>
            <div class="col-sm-6">
              <label class="form-label">Phone Number</label>
              <input type="text" class="form-control" id="phone" value="+91 90437-55555" disabled>
            </div>
          </div>
          <div class="row mb-3">
            <div class="col-sm-6">
              <label class="form-label">Location</label>
              <input type="text" class="form-control" id="location" value="Downtown street" disabled>
            </div>
          </div>
          <div class="mb-3">
            <label class="form-label">Bio</label>
            <textarea class="form-control" id="bio" rows="2" disabled>Active citizen helping to improve our community through CleanStreet</textarea>
          </div>
          <div class="text-end">
            <button type="button" class="btn btn-secondary" id="cancelBtn" style="display:none;">Cancel</button>
            <button type="submit" class="btn btn-success" id="saveBtn" style="display:none;">Save</button>
            <button type="button" class="btn btn-primary" id="editBtn">Edit</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<script>
  const editBtn = document.getElementById("editBtn");
  const saveBtn = document.getElementById("saveBtn");
  const cancelBtn = document.getElementById("cancelBtn");
  const inputs = document.querySelectorAll("#profileForm input, #profileForm textarea");
  const sidebarName = document.getElementById("sidebarName");
  const sidebarUsername = document.getElementById("sidebarUsername");
  const profileImage = document.getElementById("profileImage");
  const uploadPic = document.getElementById("uploadPic");

  // Load data from localStorage
  window.onload = () => {
    inputs.forEach(input => {
      if (localStorage.getItem(input.id)) {
        input.value = localStorage.getItem(input.id);
      }
    });
    if (localStorage.getItem("profileImage")) {
      profileImage.src = localStorage.getItem("profileImage");
    }
    sidebarName.textContent = localStorage.getItem("fullname") || "User";
    sidebarUsername.textContent = "@" + (localStorage.getItem("username") || "demo_user");
  };

  editBtn.addEventListener("click", () => {
    inputs.forEach(input => input.disabled = false);
    editBtn.style.display = "none";
    saveBtn.style.display = "inline-block";
    cancelBtn.style.display = "inline-block";
  });

  cancelBtn.addEventListener("click", () => {
    inputs.forEach(input => input.disabled = true);
    editBtn.style.display = "inline-block";
    saveBtn.style.display = "none";
    cancelBtn.style.display = "none";
  });

  document.getElementById("profileForm").addEventListener("submit", (e) => {
    e.preventDefault();

    // Save to localStorage
    inputs.forEach(input => {
      localStorage.setItem(input.id, input.value);
      input.disabled = true;
    });

    // Update sidebar
    sidebarName.textContent = document.getElementById("fullname").value;
    sidebarUsername.textContent = "@" + document.getElementById("username").value;

    alert("Profile updated successfully!");
    editBtn.style.display = "inline-block";
    saveBtn.style.display = "none";
    cancelBtn.style.display = "none";
  });

  // Profile picture upload
  uploadPic.addEventListener("change", function() {
    const file = this.files[0];
    if (file) {
      const reader = new FileReader();
      reader.onload = function(e) {
        profileImage.src = e.target.result;
        localStorage.setItem("profileImage", e.target.result);
      }
      reader.readAsDataURL(file);
    }
  });
</script>
</body>
</html>
